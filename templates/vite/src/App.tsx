import { HelloWorld } from './components/HelloWorld';

export default function App() {
  return (
    <main className="prose-sm max-w-full p-4">
      <HelloWorld />
      <p>This is a Vite + React + TS + Tailwind Template</p>
    </main>
  );
}
